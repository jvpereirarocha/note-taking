
The commands below are used to transfer or copying files through network

```
scp [source] [destination]
--> Copy *source* to *destination*

sftp host
--> Start a secure file transfer session with host.
Ex: sftp myusername@host

ftp host
--> Start a file transfer session with host
```

`ftp` is not secure as `scp` and `sftp` are